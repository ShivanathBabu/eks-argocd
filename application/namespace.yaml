apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: roboshop
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/ShivanathBabu/eks-argocd.git
    targetRevision: main
    path: namespace

  destination:
    server: https://kubernetes.default.svc
    namespace: catalogue

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true